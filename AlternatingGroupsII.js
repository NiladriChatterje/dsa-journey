var numberOfAlternatingGroups = function (colors, k) {
    groupCount = 0;
    let x;
    for (let i = 0; i < colors.length; i++) {
        x = 0;
        while (x < parseInt(k / 2)) {

        }

    }
    return groupCount;
};
