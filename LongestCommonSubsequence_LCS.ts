function longestCommonSubsequence(string1: string, string2: string, index_1: number = 0, index_2: number = 0, dp: number[] = []): number {
    if (string1.length === index_1 || string2.length === index_2) return 0;
    return 0;
}
